[package]
name = "aquic"
version = "0.0.0"
edition = "2024"

[dependencies]
tokio = {version = "1.44.1", features = ["macros", "rt", "net", "sync"]}
quiche = "0.24.1"

tracing = "0.1.41"
bytes = "1.10.1"
